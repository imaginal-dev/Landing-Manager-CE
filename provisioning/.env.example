# .env.example
# This file is an example of the environment variables required for the application to work.
# Copy it to a .env file and fill in your values.
# The .env file should not be committed to the version control system.

# --- Flask Application Core ---
# Secret key for signing sessions. Should be a long, random string.
# Can be generated with the command: python -c 'import secrets; print(secrets.token_hex())'
SECRET_KEY=your-super-secret-flask-key

# Token for accessing the Landings Manager admin panel
APP_TOKEN=your-super-secret-admin-token


# --- Mail Configuration (for sending forms) ---
# Your SMTP server address
MAIL_SERVER=smtp.example.com
# SMTP server port (usually 587 for TLS)
MAIL_PORT=587
# Whether to use TLS (true/false)
MAIL_USE_TLS=true
# Your email address (login)
MAIL_USERNAME=user@example.com
# Your email account password (or application password)
MAIL_PASSWORD=your-email-password
# Email address that will receive emails from the forms
MAIL_RECIPIENT=inbox@example.com


# --- Cloudflare Turnstile Captcha ---
# Public key (Site Key). Used in the HTML code on the landings.
CLOUDFLARE_SITE_KEY=1x00000000000000000000AA
# Default secret key (Fallback Secret Key). Used if a specific key for the domain is not found.
CLOUDFLARE_DEFAULT_SECRET_KEY=1x0000000000000000000000000000000AA

# Optional: specific secret keys for domains.
# The variable name is formed as CF_SECRET_{DOMAIN}, where dots and hyphens are replaced with underscores.
# CF_SECRET_MY_AWESOME_SITE_COM=...
# CF_SECRET_ANOTHER_DOMAIN_NET=...